# 🖥️ 新电脑设置指南

> 在另一台 Mac 电脑上运行「中文识字学习应用」的完整步骤

---

## 📍 第一步：打开终端

1. 按 `Command + 空格` 打开聚焦搜索
2. 输入 `终端` 或 `Terminal`
3. 按回车打开

---

## 📍 第二步：安装 Homebrew

复制下面这行命令，粘贴到终端，按回车：

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

> ⚠️ 安装过程中会让你输入电脑密码，输入时屏幕不会显示任何字符，这是正常的，输完按回车即可。
> 
> ⏱️ 这一步需要几分钟，耐心等待。

安装完成后，**如果终端提示你运行额外命令**（类似 `Next steps` 下面的内容），请复制执行它们。

---

## 📍 第三步：安装 Git

复制下面这行命令，粘贴到终端，按回车：

```
brew install git
```

等待安装完成。

---

## 📍 第四步：安装 Node.js

复制下面这行命令，粘贴到终端，按回车：

```
brew install node
```

等待安装完成。

---

## 📍 第五步：验证安装成功

依次运行以下三行命令：

```
git --version
```

```
node --version
```

```
npm --version
```

如果每行都显示版本号（例如 `git version 2.39.2`），说明安装成功！✅

---

## 📍 第六步：下载项目代码

复制下面这行命令，粘贴到终端，按回车：

```
git clone https://github.com/hitbastank/chinese-literacy-app.git
```

这会把代码下载到当前目录。

---

## 📍 第七步：进入项目文件夹

```
cd chinese-literacy-app
```

---

## 📍 第八步：安装项目依赖

```
npm install
```

> ⏱️ 这一步需要1-2分钟，会下载项目需要的所有库。

---

## 📍 第九步：启动项目

```
npm run dev
```

你会看到类似这样的输出：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

---

## 📍 第十步：打开浏览器访问

打开 Safari 或 Chrome，在地址栏输入：

```
http://localhost:5173
```

🎉 **你应该能看到中文识字学习应用了！**

---

## 🛑 如何停止项目

在终端按 `Control + C`

---

## 🔄 如何获取最新代码

如果你在另一台电脑更新了代码，在这台电脑运行：

```
git pull
npm install
npm run dev
```

---

## ❓ 常见问题

### Q：提示 `command not found: brew`
A：Homebrew 没装好。重新执行第二步，注意看有没有提示 "Next steps"。

### Q：提示 `command not found: node`
A：Node.js 没装好。重新执行第四步。

### Q：`npm install` 报错
A：可能是网络问题，再运行一次试试。

---

## 📞 需要帮助？

在 Antigravity 中打开这个项目，直接问我就行！
