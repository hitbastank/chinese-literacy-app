## Design Spec（通用约束版）

> 适用场景：项目采用 vibe coding，暂无完整视觉稿。本文件用于统一设计语言与行为边界，避免 AI 按平均经验自由发挥。

---

## 1. Design Tokens（语义化规范）

### 1.1 颜色（以 `src/index.css` 为准）

#### 背景与中性
- `--bg-charcoal-900`: `#1E1F22`（页面主背景）
- `--bg-charcoal-800`: `#2A2C2F`（模块背景）
- `--bg-charcoal-700`: `#34373B`（层级抬升）
- `--bg-panel-gloss`: `#404349`（高光面板）

#### 描边与分割
- `--stroke-steel-700`: `#3A3D41`
- `--stroke-steel-600`: `#4A4E54`
- `--stroke-highlight`: `#565A61`

#### 文本
- `--text-base`: `#C9CDD3`（正文）
- `--text-muted`: `#9EA3A9`（次要信息）
- `--text-inverse`: `#0E1114`（浅底深字）

#### 品牌与状态
- `--accent-cyan`: `#00E5FF`（主强调）
- `--accent-teal`: `#19FFC4`（次强调）
- `--state-success`: `#20E39A`
- `--state-warning`: `#F5B64A`
- `--state-error`: `#FF5A6E`
- `--state-info`: `#4FD1FF`

### 1.2 字体与排版
- 标题：保持当前科技/像素混合风格，优先可读性。
- 正文：中文优先清晰可读，不使用过度装饰字体。
- 字号策略：移动端不低于 14px，主内容建议 16px+。

### 1.3 间距与圆角
- 模块间距：优先 12px / 16px / 24px 三级节奏。
- 圆角：卡片与模块保持中等圆角（与现有 glass 模块一致）。
- 内边距：卡片内容优先保证触控友好（按钮高度 >= 40px）。

### 1.4 阴影与光效
- 使用已有变量：`--shadow-outer-*`、`--shadow-inset-*`、`--glow-cyan-*`。
- 光效用于“可交互元素”提示，不用于大面积持续发光。

---

## 2. 产品逻辑（设计视角）

### 2.1 目标用户
- 6-8 岁儿童为主，兼顾家长陪学场景。

### 2.2 核心功能优先级
- P0：中文学习主流程（课程 -> 学习 -> 巩固 -> 进度反馈）
- P1：英语分级学习（当前以 Grade 1 为主）
- P2：激励与长期机制（成就、打卡、家长报告）

### 2.3 内容优先级
- 先保证“可学、可练、可复习”的闭环完整。
- 再扩展内容规模（如 Level 10 完整化、英语 Grade 2+）。

---

## 3. 规则约束（能做什么/不能做什么）

### 3.1 可以做
1. 在既有视觉语言内优化可读性与交互反馈。
2. 在既有页面结构内增强学习效率（如提示文案、状态显示）。
3. 依据 PRD 增补已确认的范围内功能。

### 3.2 不能做
1. 未经 PRD 明确，不新增独立产品模块。
2. 不切换整体视觉主题（例如突然改为纯卡通浅色系）。
3. 不为“酷炫”牺牲儿童可读性和交互清晰度。
4. 不引入与当前用户年龄不匹配的复杂交互范式。

### 3.3 文案约束
- 学习文案保持短句、积极、鼓励式语气。
- 错误提示避免负反馈，优先引导“再试一次”。

---

## 4. 情绪目标（Emotion Goal）

### 4.1 希望用户感受到
- 安全：界面稳定、可预期。
- 有趣：学习像闯关，不是考试。
- 成就：每次操作都有正反馈。
- 轻松：信息不过载，不焦虑。

### 4.2 交互体验原则
1. 一步一反馈：点击后要有明确响应。
2. 低挫败：答错后给鼓励与下一步建议。
3. 可恢复：随时可返回、重试、继续学习。

---

## 5. AI 执行说明（设计侧）

1. AI 设计修改必须先检查本文件与 `PROJECT_BIBLE.md`。
2. 若需求超出本文件约束，先补文档再改代码。
3. 未定义的视觉细节，优先复用现有组件样式，不另起一套风格。
