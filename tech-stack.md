## Tech Stack Spec

> 目标：明确“当前怎么实现”与“哪些不能随意改”，避免 AI 中途切技术方案。

---

## 1. 当前技术栈（As-Is）

### 1.1 前端
- 框架：`React 18`
- 构建：`Vite 5`
- 路由：`react-router-dom 6`
- 样式：原生 CSS（以 `src/index.css` 设计变量为核心）

### 1.2 学习能力与服务
- 汉字笔画：`hanzi-writer`
- 语音：浏览器语音 + 本地服务回退链（项目内已实现）

### 1.3 数据与同步
- 本地存储：`localStorage`
- 云同步：`Firebase Firestore`
- 同步策略：应用启动合并云端/本地；本地写入后异步同步云端

---

## 2. 架构边界（Architecture Boundaries）

### 2.1 页面路由（当前）
- `/`：首页
- `/learn`：基础学习页
- `/games/*`：游戏页
- `/progress`：进度页
- `/curriculum`：课程目录
- `/lesson/:lessonId`：课程学习详情
- `/english`：英语学习
- `/restore`：数据恢复

### 2.2 目录职责
- `src/pages`：页面级业务
- `src/components`：可复用组件
- `src/data`：课程与题库数据
- `src/utils`：语音、存储等工具能力

---

## 3. 环境与数据安全约束

### 3.1 Firebase 环境隔离（强制）
- 开发环境写入集合：`dev_users`
- 生产环境写入集合：`users`
- 由域名白名单判断生产环境（见 `src/firebase.js`）

### 3.2 禁止事项
1. 禁止绕过 `validateWritePermission` 直接写入非当前环境集合。
2. 禁止在未评审前修改生产域名白名单逻辑。
3. 禁止在无迁移方案的情况下改变存储数据结构。

---

## 4. 变更策略（Change Policy）

### 4.1 可接受变更
- 小版本依赖升级（兼容前提下）
- 既有模块性能优化与错误修复
- 在既有栈内增加功能（不换框架）

### 4.2 需评审后才能改
- React / Vite / Router 主框架替换
- Firebase 替换为其他后端
- 路由结构大改、数据模型大改

---

## 5. AI 执行规则（技术侧）

1. AI 默认在现有技术栈内实现需求，不自行引入新框架。
2. 若确需新增依赖，必须先说明必要性与替代方案。
3. 若需求可能触发架构调整，先更新本文件与 PRD，再进行代码变更。
